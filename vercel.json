{
  VAMOS ATUALIZAR DIRETAMENTE NO GITHUB!**

**Como o git aqui está com restrições, vamos fazer as mudanças direto no GitHub:**

---

## 📂 **PASSO 1: REMOVER O index.html SIMPLES**

**No GitHub:**
1. **Vá** para: `github.com/hangredmatos70-6997s-projects/area51-bar-system-2024`
2. **Clique** no arquivo `index.html` (se existir na raiz)
3. **Clique** no ícone da lixeira 🗑️ 
4. **Commit**: "Remove arquivo simples"

---

## 📝 **PASSO 2: ATUALIZAR vercel.json**

**No GitHub:**
1. **Clique** no arquivo `vercel.json`
2. **Clique** em "Edit" (ícone do lápis)
3. **Substitua** todo o conteúdo por:

```json
{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "builds": [
    {
      "src": "server/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/index.ts"
    },
    {
      "src": "/assets/(.*)",
      "dest": "/dist/public/assets/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/dist/public/index.html"
    }
  ],
  "env": {
    "DATABASE_URL": "@database_url",
    "SESSION_SECRET": "@session_secret"
  },
  "functions": {
    "server/index.ts": {
      "maxDuration": 30
    }
  }
}
